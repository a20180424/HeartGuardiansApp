# Default API base URL, used in both dev and production builds.
# PUBLIC value (baked into the client bundle) — safe to commit.
# Override locally with a .env.local file (gitignored) if needed.
VITE_API_URL=https://heartguardians-api.a20180424.workers.dev
