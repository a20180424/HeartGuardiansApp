# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Unity가 이미지 반입 시 만드는 사이드카 파일 — 이 프로젝트엔 불필요.
*.meta

# Env files — never commit (set vars in the platform dashboard instead).
# .env.example is kept as documentation.
.env
.env.*
!.env.example

# DEV 테스트 계정 (no-build MPA 용, .env.local 의 HG_TEST_* 대체) — 커밋 금지.
www/dev-config.js

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Temporary 3D test asset (Planet3 3D test only — not used in production)
public/assets/3d/azure_nexus.glb

# Playwright MCP verification artifacts (screenshots, console logs)
.playwright-mcp/

# Claude Code — 개인 로컬 설정만 제외. 공유 설정(.mcp.json, .claude/*.config.json)은 커밋한다.
.claude/settings.local.json

# Superpowers SDD scratch (ledger, task briefs/reports, review diffs)
.superpowers/

# Source art staging area — canonical copies live in src/assets/
mytemp/

# Transient command logs from tooling runs
build-output.txt
lint-output.txt
